<div class="ui mini modal">
    <div class="header">
        {{ title }}
    </div>

    <div class="description">
        <p>
            {{ text }}
        </p>
    </div>

    <div class="actions">
        <button class="ui black deny button">
            Annuler
        </button>
        <a href="{{ url }}" class="ui positive right labeled icon button">
            Confirmer
            <i class="checkmark icon"></i>
        </a>
    </div>
</div>
